import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora de IMC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
